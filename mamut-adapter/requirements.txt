# Capa MAMUT del optimizador (server.py) = stdlib puro, ZERO-DEP.
#
# Desde MAM-236 (2026-08-25) el gate de licencia NO corre: `server.py` no importa
# `license_guard` y no hay ningun dependiente externo para levantar el adapter.
#
# `license_guard.py` sigue en el repo, sin invocarse. Si alguien lo corre suelto
# (`python license_guard.py`) o corre sus tests, necesita PyJWT con el extra
# `crypto` para EdDSA/Ed25519 -- instalalo a mano, no hace falta para servir:
#   pip install "pyjwt[crypto]>=2.4"
