//! hydro-norms — Norm profiles and validator.
//!
//! Deserializes the 11 norm JSON profiles (CONAGUA_MX, EPA_US, ABNT_BR, …)
//! and validates pipe networks against them. Violation counts are EXACT-match
//! tested against the Python oracle.

#[cfg(test)]
mod tests {
    #[test]
    fn workspace_member_compiles() {
        // The test passing confirms compilation succeeded.
    }
}
